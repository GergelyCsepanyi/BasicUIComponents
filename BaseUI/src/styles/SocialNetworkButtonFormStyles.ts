import {StyleSheet} from 'react-native';

const SocialNetworkButtonFormStyles = StyleSheet.create({
  containerStyle: {
    // height: 10,
    // paddingTop: 20,
    // paddingBottom: 30,
    marginBottom: 15,
    flexDirection: 'row',
  },
});

export default SocialNetworkButtonFormStyles;
